/**
 * Barrel export para todos os tipos
 * Re-exporta tipos organizados por domínio para manter compatibilidade
 */

// Tipos do Dashboard
export * from './dashboard';

// Tipos de Autenticação
export * from './auth';

// Tipos de Marketing
export * from './marketing';

// Tipos de RPC (já existe em rpc.ts)
export * from './rpc';

// Tipos de Cache (já existe em cache.ts)
export * from './cache';